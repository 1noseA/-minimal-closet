<%= attachment_image_tag @calendar, :day_image, format: 'jpeg',  size: "300x400" %>

<%= @calendar.text %>

<%= link_to "編集する", edit_user_calendar_path(current_user,@calendar),class: "btn btn-change" %> 
<%= link_to "削除する", user_calendar_path(current_user,@calendar),method: :delete, class: "btn btn-delete" %>
<%= link_to "戻る", user_calendars_path(current_user) ,class: "btn btn-delete" %>