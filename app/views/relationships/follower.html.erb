<div class="container">
  <div class="row">
    <div class="col-md-12 mx-auto">
    <h4>フォロー中</h4>
      <% if @users.count > 0 %>
        <%= render '/users/followlist', users: @users %>
      <% else %>
        <p>ユーザーはいません</p>
      <% end %>
    </div>
  </div>
</div>